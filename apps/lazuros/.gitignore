.env
# The filled-in deployment config (real WoL MAC / node IPs) — copied from a committed
# template (deployment.example.json / deployment.jag.json) and mounted at runtime.
deployment.json
*.db
*.db-shm
*.db-wal
node_modules/
dist/
build/
__pycache__/
*.pyc
.DS_Store

# Private keys / certs
*.pem
*.key
*.p8
*.p12
*.pfx
