beautifulsoup4>=4.12
lxml>=5.0
pdfplumber>=0.11
pydantic>=2.0
typer>=0.12
jsonschema>=4.0      # IR validation (library_cli)
requests>=2.31       # AI split fallback (library_cli --ai)
httpx>=0.27          # for --push-to API calls
pdf2image>=1.17      # OCR fallback (--ocr flag)
pytesseract>=0.3     # OCR fallback (requires tesseract binary)
Pillow>=10.0         # required by pdf2image
