// SylibOS uses the shared preferences hook verbatim. SylibOS-specific preset
// schemes live separately in ../lib/theme (useTheme / applyScheme).
export { useJkOSPreferences } from '@jkos/auth-client';
