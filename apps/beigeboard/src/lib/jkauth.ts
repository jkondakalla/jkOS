// BeigeBoard consumes the shared jkOS auth + preferences contract.
// Implementation lives in @jkos/auth-client (types, defaults, profile client,
// normaliseTheme, applyTheme, AUTH_URL). Do not re-add local copies here.
export * from '@jkos/auth-client';
