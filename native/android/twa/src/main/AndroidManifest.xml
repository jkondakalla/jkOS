<?xml version="1.0" encoding="utf-8"?>
<!--
  Every TWA shell (jkOS, KourOS). Everything that differs between them — the start URL, the
  host, the claimed origins, the name, the bar colour, the shortcuts — is a resource in the
  variant's GENERATED res/ (native/gen-native.mjs). This file is the shape they share.

  NO PERMISSIONS, NO CODE. Chrome holds the network, the cookies, the media session and the
  lock-screen controls; this APK only asks Chrome to open a verified origin without its URL
  bar. androidbrowserhelper merges in one <queries> entry (VIEW https BROWSABLE) so it can
  find a browser to ask.

  The URL bar disappears only if the origin's /.well-known/assetlinks.json names this
  package and its signing key (infra/nginx/assetlinks.json, via native/android/signing.mjs)
  AND this APK's asset_statements claims the origin. Either half missing: the app works and
  shows a URL bar forever, with no error anywhere.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Translucent.NoTitleBar">

        <meta-data
            android:name="asset_statements"
            android:resource="@string/asset_statements" />

        <activity
            android:name="com.google.androidbrowserhelper.trusted.LauncherActivity"
            android:alwaysRetainTaskState="true"
            android:exported="true"
            android:label="@string/app_name">

            <meta-data
                android:name="android.support.customtabs.trusted.DEFAULT_URL"
                android:value="@string/start_url" />
            <meta-data
                android:name="android.support.customtabs.trusted.ADDITIONAL_TRUSTED_ORIGINS"
                android:resource="@array/additional_trusted_origins" />
            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                android:resource="@color/shell_bar" />
            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
                android:resource="@color/shell_bar" />
            <!-- No Chrome-class browser installed: open a Custom Tab in whatever browser there
                 is, rather than falling back to an in-app WebView this APK would then own. -->
            <meta-data
                android:name="android.support.customtabs.trusted.FALLBACK_STRATEGY"
                android:value="customtabs" />
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Links to the start origin open in this app (Plex → Plexamp: a kouros.jkos.net
                 link opens KourOS). Verified against the same assetlinks.json. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="@string/host"
                    android:scheme="https" />
            </intent-filter>
        </activity>
    </application>
</manifest>
