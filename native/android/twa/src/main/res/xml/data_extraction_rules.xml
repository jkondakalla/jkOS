<?xml version="1.0" encoding="utf-8"?>
<!-- Nothing leaves the device. allowBackup="false" alone does NOT stop Android 12+'s
     device-to-device transfer, and a WebView's cookie jar (app_webview/, under the data root)
     holds a live jkOS session. A session is re-established by signing in, never copied. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
